
# ASP.NET (.NET Framework) MVC Practicals

This single repository contains all practicals of ASP.NET (.NET Framework) MVC.


## Notes

- Each practical is separated into single solution and each tests of that practical is separated into projects placed into single solution.

- Each test inside particular practical contains README.MD file specifying how each test is structured into code.